{
    "name": "root",
    "private": true,
    "scripts": {
        "build": "cd ./packages/api-schema && npm i && npm run build && cd ../server && npm i && npm run build && cd ../client && npm i && npm run build",
        "deploy": "firebase deploy"
    },
    "dependencies": {
        "firebase-tools": "6.1.1"
    }
}